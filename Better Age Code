/* Copyright Charlie Mitchell 2017, 2026 */

#include <iostream>
using namespace std;

int main()
{
    int age;
    cout<<"enter your age"<<endl;
    cin>>age;
    
    if ((age >= 13)&&(age <=200)) {
        if(age >= 21) {
            cout << "Adult";
        }
        else {
            cout << "Teenager";
        }
    }
    else {
        if (age > 0) {
            cout << "Child";
        }
        else {
            cout << "Something's wrong here, hmm...";
        }
    }

    system("\n pause");
    return 0;
}
